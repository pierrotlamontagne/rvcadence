[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rvcadence"
version = "0.3.0"
description = "Greedy observing-calendar scheduler for radial-velocity follow-up campaigns"
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [{ name = "Pierrot Lamontagne", email = "pierrotlamontagne31415@gmail.com" }]
keywords = ["radial-velocity", "exoplanets", "observation-scheduling", "astronomy"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Astronomy",
]
dependencies = ["astropy>=5.0", "matplotlib>=3.5", "numpy>=1.20"]

[project.optional-dependencies]
# astro and plot are part of the base install; they stay declared so existing
# rvcadence[astro] / rvcadence[plot] pins keep resolving.
astro = []
plot = []
examples = ["manim>=0.18"]
dev = ["pytest>=7.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/rvcadence"]
